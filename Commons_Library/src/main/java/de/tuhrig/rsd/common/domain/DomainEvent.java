package de.tuhrig.rsd.common.domain;

/**
 * A domain event informs consumers about a state change which
 * has happened in our domain. The state change has already
 * happened and the domain event represents a completed action
 * which has taken place in the past.
 */
public interface DomainEvent {
}
