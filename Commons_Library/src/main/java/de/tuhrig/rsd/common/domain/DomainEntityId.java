package de.tuhrig.rsd.common.domain;

/**
 * Every domain entity is identified by its ID. If we have two
 * domain entities with the same ID, both objects are considered
 * as being equal. This interface marks such a domain identifier.
 */
public interface DomainEntityId {
}
