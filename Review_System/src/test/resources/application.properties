# Generate a new Broker for each application context in tests,
# otherwise there will be multiple listeners registered on the same broker
#spring.activemq.broker-url = vm://broker${random.value}?broker.persistent=false
#spring.activemq.user       = admin
#spring.activemq.password   = admin
logging.level.root         = INFO

# running against local activemq
spring.activemq.broker-url=tcp://localhost:61616
spring.activemq.user=admin
spring.activemq.password=admin

#spring.event.pub-sub-domain=true
